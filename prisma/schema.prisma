generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://unifei:unifei@127.0.0.1:3306/linkedin_data"
}

model company {
  id           Int     @id
  name         String? @db.VarChar(255)
  description  String? @db.Text
  company_size Int?
  state        String? @db.VarChar(255)
  country      String? @db.VarChar(255)
  city         String? @db.VarChar(255)
  zip_code     String? @db.VarChar(20)
  address      String? @db.VarChar(255)
  url          String? @db.VarChar(255)
}

model job {
  id              Int      @id @default(autoincrement())
  job_id          String?  @db.VarChar(255)
  company_name    String?  @db.VarChar(255)
  title           String?  @db.VarChar(255)
  description     String?  @db.Text
  location        String?  @db.VarChar(255)
  company_id      Int?
  med_salary      Float?   @db.Float
  remote_allowed  Boolean?
  work_type       String?  @db.VarChar(255)
  application_url String?  @db.Text
  expiry          String?  @db.VarChar(255)
}

model profile {
  id         Int     @id @default(autoincrement())
  first_name String? @db.VarChar(255)
  last_name  String? @db.VarChar(255)
  full_name  String? @db.VarChar(255)
  city       String? @db.VarChar(255)
  state      String? @db.VarChar(255)
  languages  String? @db.VarChar(255)
  school     String? @db.VarChar(255)
  company_id Int?
}
